
import { ActivityIndicator } from "react-native";

const Loading = () => {
    return (
        <ActivityIndicator
        size="large"
        color="#0000ff"
        className="mt-10 self-center"
        />
    );
};
export default Loading;